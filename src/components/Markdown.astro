---
import { marked } from '@components/blogalog/utils/markdownit.js';
let content = await Astro.slots.render('default');
const html = marked(content || '(no content rendered)');
---

<Fragment set:html={html} />